using System;

namespace GameData.network.util.enums
{
    /// <summary>
    /// This Enum describes the type of websocket endpoint, so whether it is the websocket server or the client
    /// </summary>
    public enum WebSocketType
    {
        WEBSOCKET_CLIENT,
        WEBSOCKET_SERVER
    }
}
