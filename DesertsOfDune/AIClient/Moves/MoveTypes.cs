using System;
namespace AIClient.Moves
{
    /// <summary>
    /// enumeration of all "move" types
    /// </summary>
    public enum MoveTypes
    {
        END_TURN,
        MOVE_LEFT_UP,
        MOVE_UP,
        MOVE_RIGHT_UP,
        MOVE_RIGHT,
        MOVE_RIGHT_DOWN,
        MOVE_DOWN,
        MOVE_LEFT_DOWN,
        MOVE_LEFT,
        MOVE_WITH_HELI,
        ATTACK,
        COLLECT_SPICE,
        TRANSFER_SPICE,
        KANLY,
        FAMILY_ATOMICS,
        SPICE_HOARDING,
        VOICE,
        SWORD_SPIN
    }
}
